/**
 * Border radius (px) for subject GlassCards and the theory/lab corner accent.
 * GlassCard uses literal `!rounded-[12px]` so Tailwind emits the class — keep in sync with this value.
 */
export const SUBJECT_CARD_RADIUS_PX = 12;
